<div {{bind-attr class=":panel-body :conversation-content :scroll-v-conversation conversation.room:room"}}>
    {{#each conversation.content}}
        <div class="row">
            <div class="col-sm-3 user-name">
                {{#unless info}}
                    {{user.name}}:
                {{/unless}}
            </div>
            <div class="col-sm-9 content">
                <span class="pull-right meta-info">
                    {{#if encrypted}}
                        <i class="fa fa-lock"></i>
                    {{/if}}
                    {{formatDate date "hh:mm a"}}
                </span>
                {{#if info}}
                    <p class="info">{{content}}</p>
                {{else}}
                    {{renderConversationContent content}}
                {{/if}}
            </div>
        </div>
    {{/each}}
</div>